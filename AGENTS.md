# 프로젝트 에이전트 작업 규칙

이 파일은 저장소 전체에 적용됩니다. 파일을 바꾸는 작업은 반드시 아래 하네스를 사용합니다. 읽기 전용 조사와 설명만 하는 작업은 하네스를 시작하지 않아도 됩니다.

## Codex에서 활성화

- Codex는 프로젝트 루트의 \`AGENTS.md\`를 새 실행 또는 새 세션을 시작할 때 읽습니다. 이미 실행 중인 에이전트에는 자동으로 소급 적용되지 않습니다.
- 프로젝트 루트 \`/Users/yong/Workspaces/joseon-night\`에서 새 세션을 시작합니다.
- 프로젝트를 신뢰한 뒤 \`/hooks\`에서 \`.codex/hooks.json\`의 현재 해시를 검토하고 신뢰합니다. 신뢰하지 않은 프로젝트 훅은 실행되지 않습니다.
- 기존 작업을 이어받는 첫 실행은 \`--adopt-existing\`을 사용하고, 계획에 기존 변경의 범위와 가정을 적습니다.

## 작업 전에 읽을 문서

작업 범위에 맞게 다음 문서를 먼저 읽습니다.

- \`README.md\`: 실행 방법과 전체 검증 명령
- \`개발가이드.md\`: 아키텍처, 패키지, 테스트, 네이밍 규칙
- \`개발계획.md\`: 목표, 완료 기준, 범위 밖 항목
- 게임 규칙 변경: \`도메인모델.md\`
- 화면 변경: \`화면디자인.md\`와 \`ASSETS.md\`

문서끼리 충돌하거나 사용자 요청을 둘 이상으로 해석할 수 있으면 구현 전에 사용자에게 확인합니다.

## 필수 하네스 흐름

메인 에이전트만 하네스 상태를 전이합니다. 서브에이전트는 맡은 범위의 조사·구현·테스트 결과와 정확한 명령을 메인 에이전트에 반환합니다.

1. 계획
   - 새 작업은 \`./scripts/harness begin --request "<요청 요약>"\`으로 시작합니다.
   - 이미 변경이 진행 중인 작업을 넘겨받으면 \`--adopt-existing\`을 함께 사용합니다.
   - 사용자가 하네스 자체 변경을 명시적으로 요청한 경우에만 \`--harness-maintenance\`를 함께 사용합니다.
   - \`./scripts/harness plan\`에 중요한 가정, 검증 가능한 완료 기준, 최소 구현 단계, 필요 시 수동 확인을 기록합니다.
   - 계획이 승인되기 전에는 제품 파일을 편집하지 않습니다.
2. 구현
   - 요청을 해결하는 가장 작은 변경만 합니다.
   - 기존 스타일을 보존하고 관련 없는 리팩터링을 하지 않습니다.
   - 버그 수정은 가능하면 실패하는 재현 테스트를 먼저 만듭니다.
3. 테스트
   - 먼저 변경 범위에 맞는 표적 테스트를 실행합니다.
   - 이어서 \`./scripts/harness run test\`를 실행합니다.
4. 검증
   - 계획에 수동 확인이 있으면 \`./scripts/harness evidence\`로 실제 결과를 기록합니다.
   - \`./scripts/harness run verify\`를 실행합니다. 이 저장소의 최종 자동 검증은 CI와 같은 \`./gradlew clean check\`입니다.
5. 실패 시 계획 회귀
   - 실패 로그를 읽고 원인과 계획 변경을 \`./scripts/harness revise --cause "..." --change "..."\`로 기록합니다.
   - 수정 후 테스트와 검증을 처음부터 다시 실행합니다.
   - 실패 상태에서는 계획 수정 없이 재실행하거나 완료를 선언할 수 없습니다.
6. 통과
   - \`./scripts/harness gate\`가 0으로 끝나고 현재 파일 지문이 마지막 성공 시점과 같을 때만 완료입니다.
   - 테스트 뒤 파일이 바뀌면 이전 결과는 무효입니다.

외부 권한이나 사용자 결정 없이는 진행할 수 없을 때만 \`./scripts/harness block --reason "..."\`을 사용합니다. BLOCKED는 PASS가 아닙니다.

## 테스트와 수동 확인

- 공통 테스트: \`./gradlew test\`
- 최종 검증: \`./gradlew clean check\`
- 백엔드 실행·인프라 변경: 필요에 따라 Compose 상태와 health/status API를 확인합니다.
- JavaFX 화면·입력·렌더링 변경: macOS에서 실제 창을 열어 관련 흐름을 확인합니다.
- 수동 확인을 실행할 수 없으면 통과했다고 쓰지 말고 미검증 사유를 명시합니다.

테스트를 삭제·비활성화하거나 검증 명령과 하네스 규칙을 약화해 통과시키지 않습니다.

프로젝트 훅은 일반 파일 편집과 쓰기성 Bash 명령을 막는 실용적 가드레일입니다. 일부 특수 도구는 훅 경로를 사용하지 않을 수 있으므로 완전한 보안 경계로 보지 않습니다. CI의 \`./gradlew clean check\`, GitHub 브랜치 보호와 코드 리뷰가 최종 강제 계층입니다.

## 완료 보고

결론을 먼저 쓰고 다음을 함께 보고합니다.

- 바꾼 내용과 중요한 가정
- 실행한 테스트·검증 명령과 결과
- 하네스 gate 상태
- 실행하지 못한 수동 확인과 남은 위험
