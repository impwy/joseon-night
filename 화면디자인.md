# 조선 야행 화면 디자인

## 디자인 방향

조선의 밤, 부적, 혼불과 폐허를 소재로 한 독자적인 다크 판타지 픽셀 아트를 사용한다. 원작의 화면 배치, 명칭, 캐릭터, 이미지나 음악을 복제하지 않는다.

- 기준 창 크기: 1280×720
- 창 크기 변경: 허용
- 전투 렌더링: JavaFX `Canvas`
- 화면 오버레이: JavaFX `Pane`
- 자산 기준: 전투 스프라이트 32×32, 로비 배경 1280×720 PNG 픽셀 아트
- 확대 방식: 최근접 보간
- 맵 표현: 반복되는 달빛 폐허 타일과 플레이어 중심 카메라

## 화면 계층

```text
Root Pane
├── Lobby Background       대나무·달·풀·안개 배경
├── Login Pane             Google 로그인 시작·진행 상태
├── Registration Pane      신규 회원 닉네임 등록
├── Lobby Pane             회원 정보·전체 캐릭터 카드·시작
├── Lobby Settings Pane    로비 음소거·배경음악·효과음·화면 프레임
└── GameView
    ├── Canvas             맵·장식, 플레이어, 적, 투사체, 혼불, 상자
    ├── HUD Pane           생존 시간, 레벨, 경험치, 처치, 장비·결계, 설정
    ├── Settings Overlay   전투 위 설정과 서버 일시정지
    ├── Reward Pane        레벨업·상자 선택과 일시정지
    ├── Result Pane        패배·이탈과 재시작
    └── Connection Status  연결·재연결·오류 상태
```

Canvas는 창 크기를 따라 확장하고, 오버레이는 같은 좌표계 위에서 필요한 상태에만 표시한다. FXML은 사용하지 않고 Java 코드로 화면을 구성한다.

## 로비 화면

- 제목 `조선 야행`과 짧은 목표 설명을 보여 준다.
- 로그인 카드는 제목, 안내문, Google 공식 G 아이콘과 `Google Login` 버튼을 구분해 표시한다.
- 로그인 진행 중 버튼을 잠그고 진행 표시와 브라우저에서 완료하라는 상태를 보여 준다. 실패하면 같은 화면에서 다시 시도한다.
- 로그인 버튼 아래에는 별도의 부가 안내 문구를 표시하지 않는다. 신규 회원이면 닉네임 등록 화면으로 이동한다.
- 로그인한 회원의 닉네임과 전체 캐릭터 카드를 표시한다. 각 카드에는 캐릭터 픽셀 이미지, 이름, 설명, 시작 아이템과 고유 스킬을 표시한다.
- 잠긴 캐릭터는 어두운 실루엣과 `잠김` 상태로 표시하고 선택을 막는다. 선택한 해금 캐릭터에는 금색 테두리를 표시한다.
- 시작, 설정과 로그아웃 버튼을 제공한다. 상점 UI는 넣지 않는다.
- 설정 화면은 전체 음소거, 배경음악 음량, 효과음 음량과 자동·30·60 FPS를 제공하고 회원별로 저장한다.

## 플레이 화면

- 플레이어는 화면 중앙에 두고 월드가 카메라 반대 방향으로 이동하는 것처럼 그린다.
- 달빛 폐허 바닥 타일을 반복하고 마른 풀·잔돌/깨진 기와·얕은 균열을 고정 좌표에 그려 경계 없는 맵처럼 보이게 한다. 장식은 충돌하지 않고 모든 전투 객체보다 뒤에 둔다.
- 그림자 도깨비는 월드 바깥쪽에서 나타나 플레이어를 향해 이동한다.
- 장착 아이템별 투사체와 혼불은 배경 및 캐릭터와 구분되는 밝기로 표현한다.
- 낙뢰 방울·천뢰봉인부·뇌화신주는 이동 투사체 대신 대상 적의 머리 위에서 청백색 번개와 지면 섬광을 짧게 표시한다.
- FPS 설정은 Canvas 다시 그리기만 제한하고 이동 입력과 서버 계산 속도는 바꾸지 않는다.
- 노란·보라 상자를 구분하고 현재 뷰포트 밖의 열리지 않은 상자만 방향·거리로 안내한다.
- 공격형 아이템과 진화는 현재 Canvas와 원이 일부라도 겹치는 적만 처음 조준한다. 발사된 투사체는 화면 밖에서도 충돌을 계속 처리한다.
- 충돌 판정에 사용하는 위치와 화면에 보이는 위치가 어긋나지 않도록 같은 월드 좌표를 카메라 좌표로 변환한다.

### HUD

- 상단 왼쪽의 한 줄 패널에 `생존 시간 05:00`, 현재 레벨, 현재·필요 혼불과 처치 수를 표시한다.
- HUD는 전투 객체보다 위에 표시하되 플레이 영역을 과도하게 가리지 않는다.
- HUD 설정 버튼과 `Esc`는 같은 설정 오버레이를 열고 닫는다.

## 전투 중 설정 화면

- 설정 화면은 전투 Canvas와 현재 전투 BGM 위에 겹쳐 표시한다.
- 열 때 서버 일시정지를 요청하고 닫으면 열기 전 진행·레벨업·상자 보상 단계를 그대로 이어 간다.
- 음소거·배경음악·효과음·자동/30/60 FPS 변경은 즉시 화면과 소리에 반영하며 회원 설정으로 저장한다.
- 연결이 복구되면 현재 설정 오버레이가 열린 상태인지 서버에 다시 전송한다.

## 레벨업 화면

- 반투명 배경으로 전투 화면을 덮고 게임이 멈췄음을 보여 준다.
- 일반 강화가 최소 하나 포함된 서로 다른 보상을 최대 3개의 동일한 선택 카드로 표시한다.
- 각 카드에는 일반 능력 강화 또는 아이템 획득·강화의 이름과 효과를 적는다.
- 하나를 선택하면 오버레이를 닫고 같은 한 판을 이어서 진행한다.
- 선택 중에는 생존 시간, 적, 플레이어와 투사체가 움직이지 않는다.

## 결과 화면

- 스킬로 막지 못한 적 접촉은 `패배`, 재연결 제한 초과는 `이탈`로 표시한다. 시간 승리 화면은 표시하지 않는다.
- 생존 시간과 처치 수를 함께 보여 준다.
- 재시작 버튼은 새 `GameSession`을 만들어 로비 또는 새 한 판으로 돌아간다.
- 이전 판의 적, 투사체, 혼불, 입력과 난수 상태가 화면에 남지 않아야 한다.

## 픽셀 자산 검수 기준

- 전투 스프라이트는 정확히 32×32, 로비 배경은 1280×720 픽셀 PNG로 저장한다.
- 캐릭터, 적, 투사체, 혼불과 배경 장식의 배경은 투명하게 처리한다.
- 바닥 타일은 반복했을 때 눈에 띄는 경계가 없는지 확인한다.
- 확대 시 흐림이 없도록 이미지 스무딩을 끈다.
- 원작 자산과 혼동될 정도로 유사한 실루엣, 색 배치와 장식은 사용하지 않는다.
- 프롬프트, 후처리와 최종 확인 결과는 `ASSETS.md`에 남긴다.

## 수동 확인 항목

- 960×540, 1280×720 및 확대 창에서 화면이 잘리지 않고 경계 기준 최초 조준이 맞는다.
- WASD 입력과 실제 이동 방향이 일치한다.
- 플레이어 중심 카메라와 반복 타일이 끊기지 않는다.
- 레벨업 오버레이가 전투 전체를 멈춘다.
- 5분 이후에도 진행하고 정상 패배·재시작·첫 패배 해금 화면 전환이 정확하다.
- 로그인 진행 표시와 재시도, 전체 캐릭터 카드, 잠금·선택 상태가 정확하다.
- 로비 BGM 72초 이상, 전투 BGM 96초 이상 재생과 모니터 이동·공격음 스트레스 후에도 소리가 유지된다.
- HUD 버튼·`Esc` 설정, 서버 일시정지, 즉시 음량/FPS 반영과 같은 단계 재개가 정확하다.
- 중간 상자와 고정 배경 장식이 깜빡이거나 충돌하지 않고 성능을 저하하지 않는다.
- 실제 게임 연결이 최소 60초 동안 끊기지 않고 시간과 이동이 계속 갱신된다.
- 데스크톱 창을 닫으면 JavaFX, 데스크톱 Spring Context와 클라이언트 작업 스레드가 종료된다. 별도 `game-core` 백엔드는 백엔드 프로세스를 종료할 때 Tomcat과 Armeria를 함께 닫는다.
