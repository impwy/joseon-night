# 조선 야행 1차 개발 계획

## 목표와 완료 기준

첫 목표는 독자적인 조선 다크 판타지 게임 `조선 야행`의 5분짜리 수직 기능을 완성하는 것이다. 다음 흐름이 한 번의 로컬 실행에서 모두 동작하면 1차 개발을 완료한 것으로 본다.

```text
로비 → 이동 → 자동 공격 → 혼불 수집 → 레벨업 3택 → 전투 재개 → 승리 또는 패배 → 재시작
```

완료 기준은 다음과 같다.

- 캐릭터 1종, 무기 1종, 적 1종으로 5분간 플레이할 수 있다.
- 적 접촉 시 즉시 패배하고 5분 생존 시 승리한다.
- 레벨업 선택 중 게임 전체가 멈추며 서로 다른 보상 3개 중 하나를 선택할 수 있다.
- JavaFX `desktop-app`과 Spring Boot·Armeria `game-core`가 각각 실행되고 Armeria로 통신한다.
- 로컬 PostgreSQL·Redis·Kafka가 `game-core/compose.yaml`로 준비되고 JPA·Redis·Kafka 구성이 백엔드와 함께 부팅된다.
- `./gradlew clean check`가 로컬과 GitHub Actions에서 성공한다.

## 1단계: 프로젝트 기반

- Groovy Gradle DSL과 Wrapper 9.6.1을 사용한다.
- `game-core`, `desktop-app` 두 모듈을 구성한다.
- Java Toolchain 25를 고정한다.
- Spring Boot 4.1.0, Armeria 1.40.0, JavaFX 25.0.4와 정적 분석·테스트 도구 버전을 고정한다.
- `game-core`에 Spring Boot·Armeria 서버, JPA·PostgreSQL·Redis·Kafka·Bean Validation·보안 기반과 모든 백엔드 어댑터를 둔다.
- `desktop-app`에는 별도 Spring Boot·JavaFX 진입점, 화면 모델과 Armeria 클라이언트만 둔다.
- 두 모듈의 운영 코드는 Gradle 프로젝트 의존 없이 데스크톱 API 계약으로 통신한다. 실제 호환성을 검사하는 `contractTest`만 테스트 전용으로 두 모듈을 함께 사용한다.
- 테스트는 H2를 사용해 외부 인프라 없이 실행한다.
- 전체 코드와 프로젝트에서 직접 만든 자산에 MIT 라이선스를 적용한다.
- README, 개발 문서, 자산 제작 기록, GitHub Actions와 CodeRabbit 설정을 준비한다.

## 2단계: 게임 core

- `GameSession`, `GameRunner`, 불변 `GameSnapshot`을 중심으로 한 판의 상태와 동작을 구현한다.
- WASD 이동과 대각선 속도 보정, 최근접 적 자동 조준, 투사체 충돌과 피해를 구현한다.
- 그림자 도깨비의 직선 추적, 시간대별 난이도 상승과 혼불 드롭을 구현한다.
- 경험치 획득과 레벨 증가, 중복 없는 보상 3택, 선택 중 일시정지를 구현한다.
- 적 접촉 즉사, 5분 생존 승리와 완전한 재시작 초기화를 구현한다.
- 60Hz 전투 상태는 메모리에 유지하고, 테이블이 필요한 새 도메인만 해당 모델에 `@Entity`를 적용한다.
- provided 포트의 입력은 Bean Validation 제약과 `@ValidatedApplicationService`로 검증한다.
- 고정 난수 시드와 가상 시간으로 각 규칙을 단위 테스트한다.

## 3단계: 데스크톱과 로컬 API

- 1280×720 크기 조절 가능 JavaFX 창을 만든다.
- Canvas에 반복 맵, 플레이어, 적, 투사체와 혼불을 그리고 Pane에 로비, HUD, 레벨업과 결과 화면을 표시한다.
- `AnimationTimer`와 누적 시간을 이용해 Armeria `DesktopApiClient`로 60Hz 고정 시간 갱신을 요청한다.
- 네트워크 요청은 JavaFX Application Thread 밖의 단일 작업 스레드에서 한 번에 하나씩 처리한다.
- 지연 중 누적된 60Hz step은 최대 15개를 한 요청에 묶고 `game-core`에서 순서대로 처리해 실제 시간과 게임 시간의 차이를 보정한다.
- `GameCoreApplication`은 Armeria 서버와 데이터 인프라를 시작하고 `DesktopApplication`은 JavaFX와 Armeria 클라이언트를 시작한다.
- `game-core`의 `DesktopApi`를 `127.0.0.1:8080`에 바인딩하고 시작·입력·갱신·보상 선택과 전체 화면 상태 API를 제공한다.
- `desktop-app`은 백엔드 타입을 직접 import하지 않고 API 응답을 자체 불변 화면 모델로 변환한다.
- 추후 React 같은 웹 프런트엔드는 `game-core`의 별도 `WebGameApi`로 연결하고 같은 application provided 포트를 호출한다.
- 여러 사용자를 받기 전에는 인증된 회원 또는 게임 세션 식별자와 식별자별 `GameSession` 격리를 구현한다. 1차 수직 기능은 로컬 사용자 한 명만 지원한다.

## 4단계: 독자 자산과 화면 검수

- 도깨비 사냥꾼, 그림자 도깨비, 봉인 부적, 혼불, 달빛 폐허 바닥의 독자 시안을 만든다.
- 각 자산을 32×32 PNG로 정리하고 필요한 이미지의 배경을 투명하게 처리한다.
- 원작 이미지를 입력이나 프로젝트 파일로 사용하지 않는다.
- 생성 프롬프트, 후처리와 검수 결과를 `ASSETS.md`에 기록한다.
- macOS에서 픽셀 확대, 창 크기 변경, 레벨업 정지, 승리·패배와 재시작을 직접 확인한다.

## 5단계: 자동 검증과 전달

- ArchUnit으로 모듈 의존 방향, 헥사고날 계층과 도메인·애플리케이션 슬라이스의 순환 의존 금지를 검사한다.
- `game-core` Spring Context와 Armeria 서버를 임의 포트로 시작하는 통합 테스트를 작성한다.
- 백엔드 통합 테스트에서 H2 DataSource, JPA EntityManagerFactory, Redis ConnectionFactory와 Kafka 조립을 확인한다.
- `desktop-app`은 임시 Armeria 서버와 실제 Armeria 클라이언트로 API 계약과 연결 실패 처리를 검증한다.
- 별도 `contractTest`에서 실제 `GameCoreApplication`과 실제 `DesktopApiClient`를 연결해 서버 응답과 화면 모델의 호환성을 검증한다.
- SpotBugs를 포함한 전체 검증을 실행한다.

```bash
./gradlew clean check
```

- GitHub Actions는 push와 Pull Request에서 Temurin 25로 같은 명령을 실행한다.
- CI에서는 JavaFX GUI를 실행하지 않는다.
- 문서 기반 `main`을 만든 뒤 `feature/project-foundation` 브랜치의 일반 Pull Request로 구현을 전달한다.
- CI와 CodeRabbit 리뷰를 확인하되 Pull Request는 자동 병합하지 않는다.

## 범위 밖

1차 개발에서는 다음 기능을 추가하지 않는다.

- 회원과 실제 로그인·인가 정책
- 상점, 게임 저장과 랭킹
- Kafka 이벤트 발행·구독 시나리오
- 보물상자와 무기 진화
- 추가 캐릭터, 무기와 적
- 오디오와 설치 패키징

## 실행과 확인

```bash
./gradlew :game-core:bootRun
./gradlew :desktop-app:run
docker compose -f game-core/compose.yaml ps
curl http://127.0.0.1:8080/internal/healthcheck
curl http://127.0.0.1:8080/api/v1/game/status
```

Docker Desktop이 실행 중이어야 하며 `GameCoreApplication`이 PostgreSQL·Redis·Kafka Compose 서비스를 시작한다. 데스크톱 창을 닫으면 프런트엔드 프로세스가 종료되고, 백엔드는 별도로 종료했을 때 8080 포트를 남기지 않아야 한다.
