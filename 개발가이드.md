# 개발 가이드

## 기술 스택

- Java 25, SpringBoot 4.1.0, Armeria 1.40.0, JavaFX 25.0.4
- Gradle Wrapper 9.6.1, Groovy Gradle DSL
- Spring Data JPA, Bean Validation, Spring Security(core/crypto)
- DB: H2(테스트), PostgreSql(개발/운영)
- Lombok, JUnit 6.0.3, Mockito, ArchUnit 1.4.2, SpotBugs
- 저장소: Git
- Docker, Redis, Kafka
- PostgreSQL·JPA, Redis와 Kafka는 `game-core`의 로컬 실행 구성에 포함하고, 테스트에서는 H2를 사용한다.
- 현재 `gameplay`의 실시간 전투 상태는 메모리에서 관리한다. 영속성이 필요한 도메인이 생기면 해당 도메인 모델에 `@Entity`를 적용한다.
- 현재 웹 게임 화면은 없으므로 Spring Web·Tomcat은 사용하지 않는다. 향후 웹 프런트엔드가 생기면 Armeria 기반 `WebGameApi`를 추가한다. Lombok, Mockito, 회원과 상점은 실제 필요가 생길 때 추가한다.

## 아키텍처

- 헥사고날 아키텍처 + 도메인 모델 패턴
- 의존성 방향: 외부(Actor) -> 어댑터 -> 애플리케이션 -> 도메인 (안쪽으로만)
- ArchUnit `HexagonalArchitectureTest`로 계층 의존성을 강제한다. 위반 시 빌드에 실패한다.

### 계층

- **Domain Layer**: 핵심 비즈니스 규칙, 상위 계층에 의존하지 않음
- **Application Layer**: 포트(인터페이스)를 정의하고 구현
- **Adapter Layer**: 데스크톱 API, DB, 캐시, 메시징, 보안처럼 외부 기술과 연결되는 포트 구현

## 패키지

```text
game-core/src/main/java/kr/vamsur
├── GameCoreApplication   // 백엔드 Spring Boot 실행 진입점
├── domain
│   ├── <name>            // 애그리거트별 도메인 모델
│   └── shared            // 공유 값 객체
├── application
│   └── <name>
│       ├── provided      // 어댑터가 호출하는 기능 제공 포트
│       └── required      // 서비스가 필요로 하는 기능 요구 포트
├── adapter
│   ├── desktopapi        // JavaFX 클라이언트용 Armeria API
│   ├── webgameapi        // 향후 웹 프런트엔드용 Armeria API
│   ├── persistence       // JPA·Redis 저장 구현
│   ├── messaging         // Kafka 발행·구독 구현
│   ├── integration       // 외부 시스템 연동
│   └── security          // 인증·인가·암호화 구현
└── support
    └── stereotype        // Spring 합성 애노테이션

desktop-app/src/main/java/kr/vamsur/desktop
├── DesktopApplication    // 프런트엔드 Spring Boot·JavaFX 진입점
├── client                // Armeria 기반 DesktopApiClient
├── gameplay              // 화면 전용 불변 모델
├── configuration         // 프런트엔드 조립
└── view                  // JavaFX 화면과 입력
```

- 각 도메인/애플리케이션은 `domain/<name>`, `application/<name>/{provided,required}`로 동일 구조를 반복한다.
- `game-core`에는 `domain`, `application`, `adapter`, 공통 stereotype과 백엔드 `@SpringBootApplication`을 둔다.
- JPA·PostgreSQL·Redis·Kafka·Bean Validation·보안과 Armeria 설정은 모두 `game-core`가 소유한다.
- `desktop-app`에는 별도 `@SpringBootApplication`, JavaFX 화면과 `DesktopApiClient`만 둔다.
- 두 모듈의 운영 source set은 서로 프로젝트 의존성을 맺지 않고 Armeria의 데스크톱 전용 API 계약으로 통신한다. `contractTest`의 테스트 전용 의존만 예외로 둔다.
- 향후 React 등 다른 프런트엔드는 `game-core`의 별도 채널 어댑터로 연결하며 동일한 application provided 포트를 사용한다.

## 애그리거트와 애플리케이션 컴포넌트 의존관계

- `domain` 패키지 아래 첫 번째 패키지는 각각 하나의 애그리거트이다.
- `application` 패키지 아래 첫 번째 패키지는 같은 이름의 애그리거트에 대한 애플리케이션 서비스 패키지이다.
- 위에서 구분한 패키지를 **슬라이스(slice)** 라고 부른다.
- 하나의 도메인과 애플리케이션 서비스 슬라이스를 묶어 **애플리케이션 컴포넌트**라고 한다.
- 애그리거트는 다른 애그리거트 루트를 직접 참조할 수 있다(ID 값 참조). 조회 메서드와 레코드 메서드는 사용할 수 있다.
  단, 다른 애그리거트를 변경하는 메서드는 사용할 수 없다.
- 애플리케이션 서비스는 다른 애플리케이션 서비스의 기능 제공(provided) 포트 인터페이스와 DTO를 사용할 수 있다.
  단, 구현 클래스는 직접 접근하지 않는다.
- 도메인과 애플리케이션 서비스의 슬라이스는 순환 의존성을 허용하지 않는다. 단방향 의존만 가능하다.
- 1차 게임 도메인은 `gameplay` 슬라이스 하나로 시작하고, `GameSession`을 애그리거트 루트로 둔다.

## 포트 패턴

- **provided**: 드라이빙 포트 인터페이스. 예) `MemberRegister`, `MemberFinder`, `MemberAuthenticator`, `GameRunner`
  - 구현체는 `application.<name>`의 `~Service` 클래스(`MemberModifyService`, `MemberQueryService`, `GameService` 등)다.
  - `DesktopApi`와 다른 백엔드 어댑터는 구현체가 아닌 포트 인터페이스에 의존한다.
- **required**: 서비스가 의존하는 드리븐 포트 인터페이스. 예) `MemberRepository`, `EmailSender`
  - 구현은 adapter 계층 또는 의존성 역전으로 연결된 다른 애플리케이션 컴포넌트가 담당한다.
  - Spring Data 저장소 포트는 필요한 메서드만 선언하고 런타임 구현을 생성한다.
- 도메인이 필요로 하는 기술 추상화(예: `PasswordEncoder`)는 도메인에 인터페이스, `adapter/security`에 구현을 둔다.
- 1차 게임은 외부 저장소를 사용하지 않으므로 `gameplay/required` 포트가 없다. 필요해질 때만 추가한다.
- 포트 이름엔 UseCase 대신 뒤에 Publisher, Creator 등 더 의미 있는 이름을 사용한다.

## 도메인 모델 관례

- 엔티티는 `AbstractEntity` 상속 → `id`, `equals/hashCode`(HibernateProxy 안전) 공통 제공
- 생성은 필요에 따라 정적 팩터리 메서드 사용: `Member.register(...)`, `GameSession.running(...)`
- JPA용 기본 생성자는 `@NoArgsConstructor(access = PROTECTED)`
- 상태 변경은 의미 있는 도메인 메서드로만(`activate()`, `deactivate()`, `updateInfo()`, `tick()`). setter 금지
- 불변식은 메서드 진입 시 `Assert.state(...)` / `Assert.isTrue(...)` 또는 프레임워크에 의존하지 않는 명시적 검증으로 확인한다.
- 값 객체는 `record` + 컴팩트 생성자에서 검증(예: `Email`, `GameRules`)
- null 방지: `Objects.requireNonNull(...)`, 필요한 위치의 `@Nullable` 사용
- 패키지 기본 null 정책을 선언할 때는 `@NullMarked`(package-info.java) 적용
- 애그리거트 내부 엔티티 메서드는 package-private으로 캡슐화(예: `MemberDetail`)
- JPA와 Hibernate에 관한 규칙은 영속성 도메인을 추가하는 시점부터 적용한다.
- 테이블이 필요한 애그리거트는 `domain.<name>`의 도메인 모델에 직접 `@Entity`를 적용할 수 있다.
- 도메인은 Jakarta Persistence, Hibernate와 Spring의 검증 도구를 사용할 수 있지만 JavaFX, Armeria와 `adapter`에는 의존하지 않는다.
- `GameSession`처럼 60Hz로 변하는 런타임 애그리거트는 영속 요구가 없으므로 JPA 엔티티로 만들지 않는다.

## 애플리케이션 서비스 관례

- 표준 합성 애노테이션: `@ApplicationService` 또는 `@ValidatedApplicationService`
- 의존성은 `private final` 필드 + 생성자 주입(`@RequiredArgsConstructor`)
- 입력 검증은 포트 메서드 파라미터에 `@Valid` 부착, DTO에 Bean Validation 제약
- 도메인 규칙은 도메인에, 조율·중복 검사·트랜잭션 경계는 서비스에 둔다.
- 조회 전용 서비스와 변경 서비스 분리(`MemberQueryService` / `MemberModifyService`)
- 1차 비영속 게임은 한 `GameSession`의 명령과 스냅샷 조회가 같은 동기화 경계를 사용하므로 `GameService` 하나로 유지한다. 저장·트랜잭션 또는 독립 조회 모델이 생기면 분리한다.
- `@ApplicationService`는 Spring `@Service` 합성 애노테이션으로 컴포넌트 스캔 대상이 된다.
- 외부 입력을 받는 서비스는 `@ValidatedApplicationService`를 사용하고 provided 포트 파라미터에 `@Valid`와 Bean Validation 제약을 선언한다.
- JPA를 사용하는 변경 서비스에만 `@Transactional` 경계를 둔다. 60Hz로 호출되는 `GameService` 전체에는 트랜잭션을 적용하지 않는다.
- `GameService`는 `@ValidatedApplicationService`로 `GameRunner` provided 포트 입력을 검증하고 `GameSession`을 조율한다.
- 현재 1차 수직 기능은 로컬 사용자 한 명과 `GameSession` 하나만 지원한다.
- 여러 데스크톱 사용자나 웹 사용자를 받기 전에 인증된 회원 또는 게임 세션 식별자를 도입하고, 식별자별 `GameSession`을 격리하는 애플리케이션 서비스를 추가한다. 채널 어댑터가 singleton 게임 한 판을 모든 사용자에게 공유하게 만들지 않는다.

## 어댑터 관례

- 어댑터는 `adapter.in`, `adapter.out`처럼 방향만 나타내지 않고 `desktopapi`, `persistence`, `messaging`, `security`처럼 실제 역할로 이름을 짓는다.
- JavaFX 전용 Armeria 어댑터는 `@DesktopApiAdapter`를 적용한 `DesktopApi`로 이름을 짓고 provided 포트만 주입한다.
- 요청/응답 DTO는 `record`를 사용하고 Bean Validation 제약을 선언한다.
- `DesktopApi`는 검증 실패를 HTTP 400의 일정한 오류 응답으로 변환한다.
- `MemberRepository`는 Spring Data `Repository`를 상속하고 필요한 메서드만 선언한다.
- 저장·Redis·Kafka·보안 어댑터는 모두 `game-core`에 둔다. `desktop-app`은 이 기술들에 의존하지 않는다.
- `GameCoreApplication`은 Armeria 서버와 데이터 인프라를 시작하고 `DesktopApplication`은 JavaFX와 Armeria 클라이언트 설정을 시작한다.
- 웹 게임이 생기면 목적이 드러나는 `WebGameApi`를 추가한다. 채널과 무관한 인증·인가와 도메인 규칙을 API 어댑터에 중복 구현하지 않는다.
- 인증은 `adapter.security`에서 요청의 신원을 확인하고, 인가는 application·domain 규칙으로 처리한다. `DesktopApi`와 `WebGameApi`는 같은 신원·권한 계약을 사용한다.
- Spring MVC와 Tomcat은 별도의 요구가 생기기 전에는 추가하지 않는다.

## 데이터 전달 객체

- 요청 DTO(`MemberRegisterRequest`)는 `toInfo()`로 도메인 입력 객체(`MemberRegisterInfo`)로 변환 후 도메인에 전달한다.
- 도메인은 데스크톱 API 요청 DTO를 직접 받지 않는다.
- 백엔드 `GameSnapshot`과 `EntitySnapshot`은 `application.gameplay.provided`에 두고 `DesktopApi`에 불변 상태를 전달한다.
- `desktop-app`은 백엔드 클래스를 import하지 않고 API 응답을 자체 화면 모델로 역직렬화한다.

## 테스트 관례

- 테스트 데이터는 `MemberFixture` 등 픽스처 클래스의 정적 메서드로 생성한다.
- 포트 단위 테스트는 `provided`/`required` 패키지를 미러링하여 작성한다.
- 데스크톱 API는 임의 Armeria 포트에서 실제 HTTP 요청으로 검증하고, 애플리케이션 통합은 `@ApplicationServiceTest`를 사용한다.
- 아키텍처 규칙 위반은 `HexagonalArchitectureTest`로 자동 검증한다.
- 빌드 시 SpotBugs 정적 분석을 통과해야 한다.
- 게임 규칙은 고정 난수 시드와 가상 시간으로 테스트한다.
- Spring Context와 Armeria 통합은 임의의 loopback 포트로 테스트한다.
- CI의 `game-core` Spring 통합 테스트는 Docker Compose를 끄고 H2를 사용하며 JPA, Redis와 Kafka 조립을 확인한다.
- 로컬 실행에서는 `game-core/compose.yaml`의 PostgreSQL·Redis·Kafka 연결을 확인한다.
- `desktop-app` 테스트는 백엔드 구현에 의존하지 않고 임시 Armeria 서버와 실제 Armeria 클라이언트로 `DesktopApiClient` 계약을 확인한다.
- 별도 `contractTest`는 실제 `GameCoreApplication`과 실제 `DesktopApiClient`를 함께 실행해 두 모듈의 JSON 계약 호환성을 CI에서 확인한다. 운영 코드에는 모듈 간 직접 의존성을 추가하지 않는다.

## 네이밍

- 인바운드 포트: 동작 주체 명사(`MemberRegister`, `MemberFinder`, `GameRunner`)
- 서비스 구현: `<도메인><책임>Service`
- 아웃바운드 포트: `<도메인>Repository`, `<기능>Sender`
- 예외: `<상황>Exception`(`DuplicateEmailException`, `LoginFailedException`)
- 어댑터: 기술 방향보다 사용 목적을 드러내는 이름(`DesktopApi`, `DesktopApiClient`, `PlayRecordRepository`, `GameEventPublisher`)
- `adapter.in`, `adapter.out`, 일반적인 `webapi`처럼 목적이 불분명한 이름은 사용하지 않는다. 채널을 추가할 때는 `desktopapi`, `webgameapi`처럼 사용 목적을 드러낸다.

## 게임 클라이언트 규칙

- JavaFX `AnimationTimer`에서 누적 시간을 60Hz 고정 시간 단위로 나누고 Armeria 기반 `DesktopApiClient`로 백엔드 갱신을 요청한다.
- 네트워크 호출은 JavaFX Application Thread에서 실행하지 않는다. 전용 단일 작업 스레드에서 한 번에 하나만 처리하고 화면은 마지막 불변 상태를 그린다.
- 느린 프레임이나 네트워크 지연으로 누적된 60Hz step은 최대 15개를 한 Armeria 요청에 묶고, `game-core`가 각 step을 순서대로 처리한다.
- Canvas는 맵과 전투 객체를 그리고 Pane은 로비, HUD, 레벨업과 결과 화면을 표시한다.
- `game-core`의 Armeria는 `127.0.0.1:8080`에만 바인딩하고 `DesktopApi`의 명령과 불변 상태 응답만 제공한다.
- 레벨업 중에는 게임 시간과 모든 전투 객체를 멈춘다.
- 32×32 픽셀 자산은 이미지 스무딩을 끄고 최근접 방식으로 확대한다.

## 폴더 관리

- 새로 생성되는 폴더 및 파일은 이 프로젝트의 최상위 폴더를 벗어나지 않는다.

## 에이전트

- AI 에이전트는 위 개발 가이드를 지키며 개발하고, 도메인모델, 화면디자인 등 수정이 필요한 내용은 수정하여도 좋다.
- 개발 가이드는 벗어나지 않는다.
- https://vampire.survivors.wiki/ 모르는 것은 이곳을 참고한다.
- 기존 문서의 목적과 목차 형식을 임의로 바꾸지 않고, 필요한 내용은 기존 구조 안에서 수정한다.

## 저장소 관리

- git 저장소는 github.com/impwy/vampiresurvivors-clone
- git의 CI/CD를 이용한다.
- CodeRabbit을 이용하여 코드 리뷰를 진행한다.
- 설정된 git template을 이용한다.
- 변경 구간을 나누어 git 브랜치를 만든다.
- 브랜치별 변경 구간마다 commit을 남기고 푸시한다.
